# frozen_string_literal: true

class Logical_operators
  #here we are going to use the || and && the || stands for or and && stands for and
  male=true
  age =45
  name="Putin"
  puts (male && age)
  puts (male||age && name)
end
#note the and or operators can operate on different data types